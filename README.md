## Installation

### 1. System Dependency
This tool requires **wkhtmltopdf** to render HTML. Please install it for your operating system:
- **Windows**: [Download Installer](https://wkhtmltopdf.org/downloads.html)
- **macOS**: `brew install wkhtmltopdf`
- **Linux**: `sudo apt-get install wkhtmltopdf`

### 2. Python Setup
Once `wkhtmltopdf` is installed, run the following command in the project root:
```bash
pip install .